# BACKLOG —— 用户 2026-07-31 一次性反馈的需求与 bug（换号交接时收的）

> 来源：用户在旧号最后窗口贴的原话，已整理。**做之前先读完本页 + HANDOFF「注意事项」。**
> 逐条做完就在这里打勾并存盘推送；全部做完这个文件可以并回 HANDOFF 然后删掉。

## 新反馈（2026-08-01，v1.11.0 验收期）

> 以下两条已随 **v1.11.1** 出包（R 窗口，2026-08-01；只动程序，扩展不用重装）。

- [x] **A7 · 收信箱没写规则也收信（用户原话：「收信箱会莫名奇妙地开始收信，我没写规则也收信，
      我要的是我写什么规则他就收什么信」）**
      ✅ 已修（Q 窗口）：服务端新增 `covers()`（什么时候 + 听哪里 + 听谁；关键词那档不挡收信，
      保住 AI 复核/工作台要的现场上下文），`match()` 重构为 covers()+听内容，行为不变。
      `_handle_event` 落库前过闸：**零规则 = 零收信**；闸只挡入库和实时推送——C2 自动开帖排队
      在闸前照常，抓历史走 store_only 不过闸；扩展自检消息（channel_id 固定 "0"）豁免。
      拦下数记 `_gate_n`：诊断 [0] 结论条在「库里一条消息都没有」时点名闸，诊断包 [3] 段
      固定印「收信闸拦下 N 条」。README「各种 ID 拿法」补了第一条规则的先有鸡先有蛋说明。
      测试：e2e.py +3 条 A7 断言（零规则不收/兜底罩住才收）；e2e_multi/diag/wiz/tabs 各补
      「罩得住但永不提醒」的兜底规则适配新语义。全套 12 件 678 条绿。
- [x] **C5 · 工作台输入框空盒光标两处（用户原话：「没文字时点左上角出光标，点下面空白处
      也出光标，很奇怪，改为开始只会在点左上角时候出现光标」）**
      ✅ 已修（Q 窗口）：根因是 C4 留的 composer 点击监听——点盒子任何空白都 focus 输入框，
      空盒下等于全盒都是光标。删掉该监听，空盒只剩 textarea 单行一处可聚焦（顶左上角）。

## 新反馈（2026-08-01，后台标签页）

- [x] **C6 · 窗口必须放前台 Discord 才更新消息（用户原话：「这窗口必须放前台这 discord 网页
      才能继续更新消息，那我规则要是多的话，我要多打开几个窗口挂前台，你这程序有没有做
      这个适配优化？」）**
      ✅ 已修（V 窗口，随 **v1.11.2** 出包）：①`chrome.tabs.update(id,{autoDiscardable:false})`
      给每个 Discord 频道标签页上「不许回收」保护（onCreated/onUpdated/protectAll 三入口）；
      ②每 30 秒巡检，发现已回收/冻结的立刻后台 reload 救回，救回数记 st.rescued、
      图标悬停提示「救回过 N 个被 Chrome 回收的标签页」；③README 新一节「能不能挂在后台不管它」
      （能，后台标签页一直在收；唯二敌人是 Chrome 冻结/回收；手动兜底：
      chrome://settings/performance 把 discord.com 加进「始终保持这些网站处于活动状态」；
      一个标签页=一个频道，盯几个频道开几个后台标签页）+ 故障对照一行。
      测试：tabs_harness 桩补 tabs.update/reload/onCreated/onUpdated + 8 条 C6 断言，
      runTabs 35/35（原 27+8）；run 46/46、runFresh 16/16；服务端 12 件 678 条全绿。

## 新反馈（2026-08-01，W 窗口三条：贴纸 / 开帖开新窗口 / 开服监听）

- [x] **D2 · 自动点开新帖要新开浏览器窗口（最好最小化），不是在当前窗口多开标签页
      （用户原话：「'替我在后台自动点开新帖'这个功能得新开一个窗口，因为我发现如果
      一个浏览器多个窗口，他只会加载打开的窗口，其他窗口他不加载的，这样消息就收不到了，
      所以我需要他新开一个浏览器窗口，而不是在当前浏览器多开标签页，然后如果可以把新开的
      浏览器窗口最小化最好，这样好像也能加载discord的最新信息，总比标签页挂后台好」）**
      ✅ 已修（W 窗口 `7f186ee`，随 **v1.11.3** 出包）：openPostTab 改
      chrome.windows.create({state:"minimized", focused:false})，新帖开进新建的最小化窗口；
      个别平台 state:"minimized" 抛错自动退回 tabs.create(active:false)；关唯一标签页窗口跟着没，
      C6 防回收照旧。tabs_harness 补 windows 桩 +7 断言（runTabs 42/42）；ui/README 文案同步。
- [x] **D3 · 监听某个服务/网站是否开放（开服提醒）（用户原话：「这东西能不能搞个监听
      某个服务器是否开放？比如现在和类脑相关的那个旅途关服了，现在还没开放，之前开放过
      一会但是没人跟我说又关了，所以导致我又错过了，所以能做这个项目吗？当然如果很难的话
      就另说了」）**
      ✅ 已修（W 窗口 `ca25b59`，随 **v1.11.3** 出包）：服务端 watch 表 + watch_loop 巡检
      + watch_probe 判定（HTTP 状态 + 可选 expect/absent 关键字）；首次只落状态不吵，关→开翻脸
      走现有通知管线（notify_watch 复用 queue_local + 全部转发 hooks）；/api/watch CRUD + 手动
      /check；ui.html 新侧栏页「开服监听」；诊断包 [5.9] 段。e2e_watch.py 新建 26 条全绿。
      通用开服闹钟，盯旅途开服填它的邀请链接或网址即可。
- [x] **D1 · 有人发贴纸/表情包（Discord Sticker）时消息内容只剩一段字符串
      （用户原话：「如果用户发表情包，他只会回复一段字符串，这个可以解决吗？不过我感觉
      可能有点难，如果解决不了就算了」）**
      ✅ 已修（X 窗口 `2cf049b`，随 **v1.11.3** 出包）：接手时现状是「纯表情/贴纸消息翻译成
      [贴纸 xx]/:emoji: 占位」v1.8.0 就有，真正的缺口有两个——①**行内表情**（文字中间夹的
      <img>）被 innerText 整个丢掉，「看 :kekw: 这个」变「看  这个」，正是用户说的
      「只剩一段字符串」；②Discord 现在给同名表情码加 ~N 后缀（:cat_cry~1:），看着像乱码。
      修法：content.js 新增 textWithEmoji（克隆正文节点把表情 img 换成代码文字再取文本，
      parseLi 改走它）+ emojiCode 剥 ~N 后缀（mediaOf 的 emoji 分支同步）。
      content_test.mjs 补 3 条断言（行内表情留正文/两边文字都在/~N 剥掉）。
      浏览器侧 107 条全绿（49+16+42）。media 字段下游零使用，行为变化无影响面。

## 新反馈（2026-08-01 晚，Z 窗口两条：报错横向撑破页面 / 中转站拉不到模型）

用户原话：「他直接伸出网页了，在右边延伸了很长一条，这种太丑了，改为多行」
「而且这种报错也不正常，我用其他软件都能拉到模型，就这个监听程序拉不到」。
诊断包 0801-2242：端点 `openai → https://youzi.today/v1 | Key 已填`，报错
`ClientPayloadError…`（悬停才看得到全文），近 24 小时调模型 0 次。

- [x] **E1 · 拉模型失败的红字横向撑破页面** ✅ 已修（AA 窗口 `8a7924e`）：
      服务卡片下改为独立多行错误块（可换行可选中、长 URL 断得开、不截断不靠悬停），
      toast 同步限宽换行。 —— 根因：错误塞进 `<span class="tag">`
      （CSS `white-space:nowrap`）而它待在 `.row`（`display:flex` 不换行）里，
      一长串就把整页向右撑开，还得靠 `title` 悬停看全文（140 字截断）。
      改法：服务卡片下面给一个独立的多行错误块（可换行、可选中、长 URL 也断得开），
      不截断不靠悬停；toast 同步限宽 + 换行。
- [x] **E2 · 中转站（youzi.today）拉不到模型：ClientPayloadError** ✅ 已修（AA 窗口 `8a7924e`+`829ec2e`）：
      出网调用显式 `Accept-Encoding: identity`+UA；兜底宽容读取（原始字节、容截断、
      gzip/deflate/br/明文依次试解）；JSON 截断/前缀垃圾可抢救，实在不行从文本捞模型 id；
      删掉「多半是你代理/杀毒」的误导提示（用户根本没设代理）。
      mockllm 加 4 种不规范中转站桩，e2e 53→60 全绿。 —— 根因：
      `ClientPayloadError: 400, message='Can not decode content-encoding: …'`
      ＝对方回的压缩体 aiohttp 解不开（br 无解码器 / gzip 头骗人）。浏览器和别的软件能拉到
      是因为它们要么带 br 解码器、要么根本不要压缩。
      改法：所有出网 API 调用显式 `Accept-Encoding: identity`（要明文，别压）+ 带 UA；
      万一对方无视 identity 还压坏，兜底用 `auto_decompress=False` 自己收原始字节，
      gzip/deflate/br/明文依次试着解。错误提示同步改成人话。

## A. Bug（用户实际踩到的，优先修）

- [x] **A6 · 两个模型端点全部拉不到模型（用户 2026-08-01 报，最高优先，他因此完全用不了 AI）**
      ✅ 已修（I 窗口 `50ea64d`）：①cleanProviders 不再把掩码 key 冲成空（保存服务商抹 key→401 的根因）；
      ②base_candidates 剥单数 /model 尾巴；③空 key+外地地址人话报错、ClientPayloadError 给本机代理
      截断提示；④拉取失败 toast 放宽 140 字+悬停全文。e2e 50/50（+2 条 A6 断言）ai27 wb99 全绿。
      原诊断（dcwatch-诊断-0801-0333.txt）根因三条，留档：
      ① **保存服务商会把已掩码的 Key 抹成空**：ui.html `cleanProviders()` 把 `***` 开头的 key 变成 `''`
      再 POST /api/config，服务端只还原 `***` 开头的不还原空串 → 点「拉取模型列表」前那次自动保存
      就把 key 抹了（诊断包里 `openai → … | Key 空` 就是这么来的）→ DeepSeek 401；
      ② 他存的 base_url 是 `https://api.deepseek.com/v1/model`（尾巴多个单数 `/model`，
      `base_candidates` 只剥 `/models`）→ 请求打到 `/v1/model/models`；
      ③ 中转站（new-api 系）在他机器上 `ClientPayloadError: 400`（云侧实测同一地址正常 401 JSON，
      是他本机网络/代理把响应截断）→ 报错要给人话提示。I 窗口修。
- [x] **A1 · AI 工作台聊天刷新丢失** ✅ 已修（PLAN_A1C1.md：聊天落 SQLite 的
      wb_sessions/wb_msgs 两表，跟 C1 的多会话一起做，回归 542 全绿）。
- [x] **A2 ·「检测某个用户」完全失效** ✅ 已修（PLAN_A2.md，回归 498 全绿）。
      真 Discord 回复消息场景还没实测，发版前让用户验一条。
- [x] **A3 · 重复通知** ✅ 已修（PLAN_A3A5.md）：每个 dcwatch 标签页各弹一条网页通知
      （Windows 通知中心里和系统通知长得一样）+ server toast 一条。Notification 加了
      tag，多标签页只弹一条。真机多标签页场景发版后请用户验一次。
- [x] **A4 · 通知与转发的「出口」锁死 Discord** ✅ 已修（PLAN_A4.md）：选择器功能没坏，
      是 UX 重做——类型下拉前置加说明、出口行内新增「类型」下拉可按模板重填换服务、
      模板占位地址不再被密码框打码。只动 ui.html；演示目检 + 500/500 + 46/16 全绿。
- [x] **A5 · 关了所有本机提醒还弹系统通知** ✅ 已修（PLAN_A3A5.md）：旧标签页的
      S.cfg 停在开，/api/config 存 sinks 后服务端不广播。现在存 sinks 会 SSE 广播，
      所有开着的页面立刻生效。另：「网页通知」与「Windows 系统通知」是两条独立通道，
      已在帮助文案里说明。

## B. AI 能力（用户的核心诉求：脚本筛不住，得让模型上）

- [x] **B1 · 监听规则接 AI 判定** ✅ **全落地**。服务端（E 窗口 `5b8d34c`：规则 5 字段
      ai_check/prompt/min/ctx/human、aicheck 表、三场景 few-shot、UNREADABLE_HINTS 直接转人工、
      fail open、诊断 [4.6]）+ **界面开关（J 窗口）**：规则编辑页动作块下「AI 复核」折叠区
      （五控件 + 照抄的人话说明 + 门槛滑杆实时文案「60 = 模型有6成以上把握才提醒你」），
      规则列表加 `AI 复核` 标签。演示目检：折叠区/开关联动/滑杆文案/保存后标签出现，全过
      （banner 撞了 .banner 是 flex 的坑，长文案要包一层 span）。e2e 50 + imp 74 复跑绿。
      **遗留已清：tests/e2e_chk.py 56 条专项全绿（O 窗口，PLAN_B1 §3.3 的 13 条全钉）。**
      场景（用户原话概括，都是发 key 的人的反侦察手段）：
      ① key 中间插字符，正则筛不到；② key 后面加杂物，下一条消息说「删掉后面 xxx 才是真 key」，
      要跨消息理解；③ 用类脑社区的 `/下载` 命令发 txt 附件，key 在附件里——模型也提不到时
      要**提醒用户人工去看**。
      设计方向：规则里加「AI 复核」开关（命中宽条件后交模型判分/提取；模型说「附件/看不到」
      就发一条「需要你人工看」的通知）。注意硬规矩 6（每条模型路径都要有系统提示词）。
- [x] **B2 · 「模型不太会按格式回答」出现太频繁** ✅ 已修（A 窗口 `386a8bb` 服务端 +
      C 窗口 `18c7b62` 界面）：①ai_tag 默认提示词补了格式标尺 + few-shot 示例；
      ②**提示词后处理三档**（off / merge / strict，照酒馆 SillyTavern 的严格模式），界面下拉可选。
- [x] **B3 · 内置提示词可见、可改、可导入导出** ✅ 已修（同上两个 commit）：6 条骨架提示词
      从写死改成 cfg `sys_prompts` 可覆盖，界面「提示词抽屉」可逐条编辑 + 逐条恢复出厂；
      预设整包 ⇡导出 / ⇣导入走 dry_run 预览闸（`dcwatch.preset/1`）。
- [x] **B4 · 模型参数可调** ✅ 已修（同上）：温度 / top_p / max_tokens / 两个惩罚 / 附加 JSON 透传，
      界面「采样参数」折叠区，默认收起。
- [x] **B5 · 写一份「教模型写规则」的预设** ✅ 已修：`presets/教模型写规则.json` 内置 +
      `/api/presets` 两个只读接口 + 界面一键装，内容引用 D 节定稿的 `docs/discord-命名约定.md`。
- [x] **B6 · 「模型能调用的功能太少」** ✅ 已修（I 窗口 `9e823b2`）：工作台只读工具 +6
      （list_providers 端点与默认模型配置 / list_hooks 出口 / recent_hits 最近命中 /
      test_message 拿一条消息试算全部规则 / get_logs 运行日志 / export_extract_templates 提取模板导出），
      WB_TOOLS / WB_TEXT_PROTO / READ_HUMAN 三处同步，写工具仍只限规则那四个。
      e2e_wb 第 15 节 19 条，118/118 全绿。原需求（用户 2026-08-01 澄清「可活动空间太少」
      指的是工作台模型手里工具不够，不是上下文长度）：
- [x] **C4 · 工作台输入框第一个字不顶左上角** ✅ 已修（J 窗口）：根因不是 CSS 对齐（空 textarea
      点击落点恒为 (0,0)，目检测过），是**空盒 52px 高 + 底栏在盒内**造成的「字的位置跟点的
      地方对不上」。改成 Claude/ChatGPT 同款：空盒单行 26px 起步、随内容长高（封顶 160px）、
      发送后缩回、点盒子空白处等同点输入框、textarea 改 display:block 去掉基线死缝。
      演示模式真机目检：26→98→26、点击聚焦，全过。

## C. 功能 / 交互

- [x] **C1 · AI 工作台多开聊天** ✅ 已修（PLAN_A1C1.md：工作台左侧会话列表，
      新建/切换/改名/删除，「清空」改「新话题」，跟 A1 一起做）。
- [x] **C2 · 新帖子的可见性问题 + 自动开标签页** ✅ 全落地（v1.11.0，N 窗口收尾）
      · 服务端 F `f5cd2c5`：cfg browser 五档、threads_open、tab_orders、/api/ext/tabs、list_open_threads、诊断 [5.8]
      · 扩展 L `c13a0d8`：tabs 权限 + background 开/关/回报 + storage opened + popup 自检 + 药丸「· 自动」
      · ui.html 设置折叠区 `842d069`；e2e_tabs 53 条 `66a78a9`；content_test runTabs 27 条（chrome.tabs 桩）
      · 三处版本号 1.11.0；README「不点开的帖子看不到…」节；程序+扩展两个 zip
      给用户的直接答案：**浏览器旁听模式下，不点开的新帖子确实读不到帖内消息**——开设置页「自动点开新帖」即可。
- [x] **C3 · 模型选择的位置** ✅ 已修（C 窗口 `18c7b62`）：模型选择挪进「接入的模型服务」
      每张服务卡片里，「默认模型」区保留但只显示结果。

## D. Discord 层级命名约定（用户让 Gemini 写的，已审）✅ 已定稿

**已定稿成 `docs/discord-命名约定.md`**（2026-08-01），全项目按它说话；
B5 的内置预设（`presets/教模型写规则.json`）引用它。下面是当初审稿的结论，留档。

结论：**5 层主干是对的，可以用**，程序、文档、给模型的提示词统一按这个说话。
采纳时做四点修订：

1. **私信（DM）不在这棵树里**，是树外的第 0 类。dcwatch 规则默认 `dm=False`，文档里要写明。
2. **语音频道也有文字聊天区**（Discord 后来加的），第 3 层语音频道的描述别写死「无须打字」；
   dcwatch 按文字消息对待即可。
3. 第 3 层补一种：**公告频道（Announcement）**，行为同文字频道。
4. 与 dcwatch 数据模型的对应关系写死进约定：第 4 层 Thread 在库里也是一条
   channel（带 parent），规则里「频道」条件既可填第 3 层也可填第 4 层的 ID；
   「有人开新帖」的 kind 只在第 3 层论坛/文字频道上触发。

原文（修订前）存档在本文件的 git 历史里即可，不必单独存。定稿后写成
`docs/discord-命名约定.md` 或并入 README，且 B5 的预设要引用它。

## 优先级建议（下个窗口从这里开工）

**A1–A6、B1–B6、C1–C4、D 全部落地，v1.11.0 已出包（2026-08-01，N 窗口 C2 收尾）。**
剩下的活：

1. **等用户装 v1.11.0 真机验收**（**必须重装扩展**三步；先验自动开帖默认关着没事、开了能后台点开；
   再验拉模型 A6、B1 AI 复核、A2/A3/A5）
2. ~~e2e_chk.py 专项债~~ ✅ 已还（O 窗口：56 条全绿，13 条必钉全覆盖，全套回归复跑 675 条绿）
3. 用户新反馈（验收后大概率有）

## 流程想法（2026-07-31，用户提出，待确认后写入 AGENTS.md）
双模型分工：Claude 只做深度思考，把巨详细的实现方案写成 `PLAN_<任务名>.md` 推送到
GitHub，然后用户换 Kimi K2 来照方案写代码（快、便宜）。方案文件必须包含：改哪些文件、
函数签名、验收命令（让实现方自测）。NOW.md 指向当前 PLAN 文件。- [x] **U1 版本号对齐：扩展也 bump 到 1.11.1**（U 窗口 2026-08-01 落盘：manifest/EXT_MIN 同步 1.11.1，双 zip 重打进 release/，规矩改成三处永远钉齐）（用户 2026-08-01：「拓展和程序版本必须对上，不然不好看」——v1.11.1 只 bump 了程序，扩展还是 1.11.0。恢复 v1.11.0 的「三处钉齐」规矩：程序 VERSION / EXT_MIN / manifest 全 1.11.1，扩展 zip 重打，以后 bump 程序时扩展即使零改动也同步 bump）

- [x] **E4 工作台输入框拉高后「两处能点出光标」**（已修 v1.11.5：`.composer textarea` 禁手动拉高 `resize:none`，全局 `resize:vertical` 漏进来的手柄没了；盒子只随内容自动长高，能出光标的区域只剩文本区一处。云浏览器真实鼠标事件复现+验证）（用户原话：正常对话框只有左上角一处能出光标，
  他拉长一下对话框后就有两处点击都能出现光标，很不正常）。
  方向：composer 是「边框盒子包无边框 textarea + 底部 .cf 行」，textarea 全局规则 resize:vertical
  允许手动拉高（≤160px）；拉高后查 composer 内 textarea 之外的区域是否还能聚焦/出光标，
  以及手动 height 与 wbGrow/CSS height:26px 的打架。修法：让「能出光标的区域」永远只剩 textarea 文本区一处。

- [x] **E5 内置提示词过期，工作台模型不知道自己能干啥**（已修 v1.11.6：`WORKBENCH_SYS` 加了按侧栏逐条的功能清单，开服监听那条明确写「能」+ 硬话「不确定的功能不许断言做不到，先查再说」；实况上下文补上开服监听目标数/状态与自动开帖开关；向导提示词加「开服监听不是规则」的指路）（用户原话：「我问他你能监听服务器是否开启吗？
  他回复这个程序做不到『监听服务器是否开启』…… 但是我上面不是叫你加了这个功能吗？这模型都不知道
  自己能干啥还咋工作？」）。根因：`WORKBENCH_SYS` 是 v1.8.0 时代写的，之后加的功能
  （D3 开服监听、C2 自动开帖、B1 AI 复核、A7 收信闸、D1 行内表情）一个字都没写进去，
  模型照着老提示词老实回答「做不到」。修法：给工作台系统提示词加一节**功能清单**
  （按侧栏页逐个列，每条一句话 + 去哪儿配），并加硬话「用户问的功能你不确定就说去哪页看，
  绝不许说这个程序做不到」；实况里带上开服监听的目标数，模型能看见现状。
  以后新增功能必须同时更新这一节（写进 AGENTS 硬规矩）。

- [x] **E6 开服监听「只能监听一个」＋ 名字写死「旅途」**（已修 v1.11.6：查证后端本来就不限数量（`watch` 表 + `watch_save` 支持多条，无上限），是界面观感问题；标题改「加一个监视目标 · 想盯几个就加几个，没有数量上限」，说明补「每个目标各自按自己的间隔独立探、互不影响」，列表顶部印「在盯 N 个目标（启用 M 个）」，空态提示可继续加；`wName` placeholder「旅途」→「给它取个你认得的名字」，全仓「旅途」零残留）（用户原话：「我看到的你这个开服监听为啥
  只能监听一个，做事做周到一点，我以后要是想监听更多呢。还有这个名字不要写旅途啊，
  又不是所有服务器都叫旅途」）。先查证服务端是不是真的只支持一个（`watch` 表 + `watch_save`
  看着支持多条），若后端支持则是界面把它做得像「单个目标配置面板」——要改成明确的
  「多目标列表 + 添加」观感；`wName` 的 placeholder「旅途」换成中性示例。

- [x] **E7 输入框「发完一条又变两处光标」从根上治**（v1.11.7 已出包，AG 窗口）
      （用户原话：「我发一条消息后，他输入框灰底字消失，然后变大，然后又出现两处可以点出光标了，
      而且我需要的不是你把这个拉长输入框删了」）。修法：空值时高度归零复位 + 输入法 Enter 不漏换行；
      `resize:vertical` 还给用户（E4 那刀砍错了对象）。

- [x] **E8 诊断/界面误报「扩展是旧版」**（v1.11.7 已出包，AG 窗口）
      （用户原话：「!扩展是旧版 v1.11.4（程序要求 v1.11.6），但是我明明拓展已经更新到同版本了，
      记得修好，而不是把这个删了」）。根因：拿「任一个桥报上来的版本」跟 EXT_MIN 比，
      而没刷新的旧标签页会一直报旧版本号。修法：跟磁盘上的扩展版本比，点名是「哪个标签页要 F5」，
      并明说「扩展本身没问题、不用重装」。

## 新反馈（2026-08-02，v1.11.7 验收期）

- [x] **E9 工作台的模型只会教步骤、不会替用户动手**（v1.11.7 已出包，AH/AI/AJ 窗口）
      （用户原话：「模型：这个我没法直接帮你加——开服监听的目标需要你在「开服监听」页自己填，
      我这边伸不到那个页面……就是类似这种，就很弱智，这个模型权限太低了。我想要什么都跟模型说，
      他都能帮我配置好，要什么信息这个模型就教我怎么拿，而不是要我手动去操作」）。
      根因：`WB_TOOLS` 的写工具只有规则那四个（create/update/set_enabled/delete_rule），
      D3 开服监听、C2 自动开帖、通知开关、默认模型全是**只读或压根没有**，所以模型只能讲步骤。
      修法：给「工作台的手」补写工具 —— 开服监听 CRUD + 立刻探一次、本机提醒开关/免打扰/分数门槛、
      自动点开新帖开关与上限、从已拉到的模型里选默认模型；同步更新 `WB_WRITE_TOOLS`（确认闸）、
      `WB_TOOLS_HOWTO`、`WORKBENCH_SYS` 功能清单（硬规矩 6）。
      **红线不动**：API Key、转发出口密址、导入规则（硬规矩 10）永远不给模型写。

- [x] **F1 只能填 ID，不能填名字** —— 已做完，v1.12.0（AK→AM 窗口，2026-08-02），
      **v1.12.1 修了服务器名录**（AN/AO 窗口：频道链接被当成服务器，打服务器名搜不到候选；
      同时补 `runNames()` 32 条常驻回归 —— 就是它抓出来的）：
      扩展攒侧栏名录 → 服务端 `names` 表 + `/api/lookup` → 工作台 `find_target` 工具 →
      规则页四栏可搜索下拉。名字不唯一必须让用户确认；只认浏览器见过的。
      （用户原话：「我输入用户名字或者频道名字他没法监听啊，必须要我给 id，难道不能他自己根据名字
      去找一下，再找我确认是不是？」）。根因：规则匹配在服务端、服务端只认 ID，而**名字这层信息
      只有扩展在 Discord 网页里看得见**（侧栏项 + 消息头），两边从来没打通，就把 ID 负担丢给用户了。
      修法（四段）：
      1. **扩展攒名录**：侧栏每项的 `href` 天然带 `/channels/服务器ID/频道ID`，名字就在旁边 →
         全量抓服务器/分类/频道/帖子；消息头有显示名 + 作者 ID → 谁说过话录一个。
         **不依赖收信闸、不依赖规则**（顺手解掉 A7 的鸡生蛋：没规则时也能查到第一条规则要填的 ID）。
      2. **服务端存 + 查**：新表存名录（去重、更新 last_seen），`/api/lookup?q=名字&kind=` 模糊查，
         返回候选带**所属服务器/分类**供辨认。
      3. **工作台加手**：`find_target` 工具（按名字找频道/用户/服务器）→ 模型自己查、列候选、
         **跟用户确认后**再 create_rule。同步 `WB_TOOLS_HOWTO` + `WORKBENCH_SYS` 功能清单（硬规矩 6）。
      4. **规则页选择器**：频道/用户两栏改成可搜索下拉，打名字跳候选、选中自动填 ID，ID 框仍保留可手填。
      两个硬限制必须对用户讲明：**名字不唯一**（同名频道/顶同名的人 → 候选必须列出所属服务器/分类，
      确认这步不能省）；**只认浏览器见过的**（没进过的服务器、从没说过话的人查不到 → 退回贴链接/ID，
      贴链接程序自己抠 ID）。

## 新反馈（2026-08-02，v1.12.1 验收期）

- [x] **F2 模型分不清「用户 ID」和「频道 ID」，猜错还照建规则**（已做完，v1.12.2 已出包；AP/AQ/AR/AS 窗口）
      （用户原话：「模型好像分不清用户id和频道id，我每次都要跟他解释，他还是搞错，
      我说监听某个用户，他给我建成监听频道，我等半天没消息。他不确定的时候就该问我，
      不许直接生成」）。
      根因：Discord 的 snowflake **不管人还是频道长相完全一样**（17–19 位数字），
      光看 ID 判断不了类型。名录（F1 的 `names` 表）其实**存了 kind**，但两处漏了：
      ① 工作台的模型没被强制先查名录，可以直接把一串数字塞进 `channel_ids`；
      ② 服务端 `create_rule` / `update_rule` **收下就存，压根不校验**这个 ID 是人还是频道。
      于是规则语法合法、程序照跑，用户干等半天 —— 因为它在盯一个不存在消息的「频道」。
      修法（四道闸）：
      1. **服务端硬校验**（最关键）：每个 ID 拿去名录对 kind，跟栏位不符就**拒绝**，
         错误里点名「这 ID 是【人】，你填进了『听哪里』，人要填 author_ids」，模型自己改对；
         名录里没见过的 ID 不拦，但回一条「类型不确定，必须跟用户确认」的 note。
         分类（category）填进 channel_ids 也拦 —— 消息的 channel_id 永远不等于分类 ID，
         这是典型的「规则合法但永远不触发」。
      2. **不确定必须问**：find_target 支持**反查**（给一串数字告诉你它是人还是频道）；
         候选跨类型时提示词强制反问，不许自己挑。
      3. **让用户一眼看穿**：规则卡片和 list_rules 把 ID 翻译成「类型＋名字」
         （听谁：用户 老王 / 听哪里：频道 公告），填错栏位保存时界面红字当场点出来。
      4. 提示词写死这条纪律（RULE_FIELDS_DOC / WB_TOOLS_HOWTO / WORKBENCH_SYS / WIZARD_SYS）。
- [x] **F3 候选表 / 名录表要能折叠**（已做完，v1.12.2 已出包；AQ 窗口，跟 F2 同一版出）
      （用户原话：「不过这表得可以折叠」）。规则页的候选列表和名录表一铺开就把页面拉得很长 →
      做成可折叠块，默认收起，点标题展开，展开状态记在 localStorage；长列表限高加滚动。

## 新反馈（2026-08-02，F2/F3 验收期）

- [ ] **G1 工作台要能撤回已发的消息 + 每条加复制按钮**（未开工）
      （用户原话：「工作台能不能撤回消息，就是我发出去的消息可以撤回，就像 opencode 那样，
      很久之前的消息也能撤回。还有复制按钮」）。
      现状：工作台只能往下追加，发错一句只能靠"你忘掉上面那句"，而模型的上下文里那句还在，
      照样被它当依据；历史消息也没法复制出来贴给别人。
      修法（照 opencode 的语义：撤回 = 把对话截断到那条消息之前，之后的全部丢掉）：
      1. 每条消息记一个稳定 id 和序号（现在只有数组下标，删中间一条会串位）；
      2. `/api/wb/revert`：给一条消息 id，把它**和它之后的所有消息**从会话里删掉（含模型回复、
         工具调用记录），返回新的消息列表；被撤的内容不再进下一次请求的上下文；
      3. **模型正在思考时也能撤**：当前请求要能取消（记住在跑的 task，撤回时 cancel 掉，
         别让它答完又把答案写回一个已经被截断的会话）；
      4. 界面：消息悬浮出「撤回到这里 / 复制」两个小按钮；撤回是破坏性的，要二次确认
         （告诉用户"这条和之后的 N 条都会没"）；复制按钮走 clipboard，失败退回选中提示。
      5. 撤回后输入框里回填被撤那条用户消息的原文，方便改一改重发（opencode 就是这体验）。

- [ ] **G2 规则要能"自动开帖 → AI 判性质 → 不符就关窗，符合就守着等 key"**（未开工，要动扩展）
      （用户原话：「规则能不能做到，有新帖子出现，自动开一个最小化的窗口，然后让 AI 判断
      是不是我要的性质的帖子，比如公益区分享用不完的 key，如果是就一直监听等他发 key，
      不是就把这个浏览器窗口关掉。只把 key 发给我」）。
      现状：C2/D2 的自动开帖只会"开着"，开了就一直挂着；判断"这帖子是不是我要的"完全靠用户
      自己看，而且开多了浏览器会被一堆没用的帖子窗口塞满。
      修法（一条流水线，规则里配）：
      1. 规则加一组"开帖后处理"字段：`auto_open`（已有）+ `judge_prompt`（我要什么性质的帖子）
         + `keep_if_match`（符合就常驻监听）+ `close_if_no`（不符合就关窗）+ `judge_timeout`；
      2. 扩展开帖后把**首帖正文 + 前几条**回传服务端；服务端拿 `judge_prompt` 走一次 AI 判定
         （小 prompt、要求只回 yes/no + 一句理由，判定结果写进诊断包好复盘）；
      3. 判 no → 服务端下发关窗指令，扩展关掉那个窗口/标签页（C6 的防冻结逻辑要跳过被判死的）；
         判 yes → 这个帖子进"常驻监听名单"，后续消息照规则继续过；
      4. "只把 key 发给我"：常驻监听期间对每条新消息跑一次**取件**（正则先抓常见 key 形态，
         抓不到再问 AI"这条里有没有可用的 key，有就只回 key"），命中才提醒，提醒正文只放 key
         + 帖子链接，不再刷整段闲聊；
      5. 成本闸：判定和取件都要花 token → 规则里给每小时上限，超了退回"只提醒不判定"并告诉用户；
      6. 同步 `WORKBENCH_SYS` 功能清单（硬规矩 6）+ 工作台写工具能配这组字段 + 扩展版本三处钉齐。

