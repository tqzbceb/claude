# BACKLOG —— 用户 2026-07-31 一次性反馈的需求与 bug（换号交接时收的）

> 来源：用户在旧号最后窗口贴的原话，已整理。**做之前先读完本页 + HANDOFF「注意事项」。**
> 逐条做完就在这里打勾并存盘推送；全部做完这个文件可以并回 HANDOFF 然后删掉。

## A. Bug（用户实际踩到的，优先修）

- [x] **A1 · AI 工作台聊天刷新丢失** ✅ 已修（PLAN_A1C1.md：聊天落 SQLite 的
      wb_sessions/wb_msgs 两表，跟 C1 的多会话一起做，回归 542 全绿）。
- [x] **A2 ·「检测某个用户」完全失效** ✅ 已修（PLAN_A2.md，回归 498 全绿）。
      真 Discord 回复消息场景还没实测，发版前让用户验一条。
- [x] **A3 · 重复通知** ✅ 已修（PLAN_A3A5.md）：每个 dcwatch 标签页各弹一条网页通知
      （Windows 通知中心里和系统通知长得一样）+ server toast 一条。Notification 加了
      tag，多标签页只弹一条。真机多标签页场景发版后请用户验一次。
- [x] **A4 · 通知与转发的「出口」锁死 Discord** ✅ 已修（PLAN_A4.md）：选择器功能没坏，
      是 UX 重做——类型下拉前置加说明、出口行内新增「类型」下拉可按模板重填换服务、
      模板占位地址不再被密码框打码。只动 ui.html；演示目检 + 500/500 + 46/16 全绿。
- [x] **A5 · 关了所有本机提醒还弹系统通知** ✅ 已修（PLAN_A3A5.md）：旧标签页的
      S.cfg 停在开，/api/config 存 sinks 后服务端不广播。现在存 sinks 会 SSE 广播，
      所有开着的页面立刻生效。另：「网页通知」与「Windows 系统通知」是两条独立通道，
      已在帮助文案里说明。

## B. AI 能力（用户的核心诉求：脚本筛不住，得让模型上）

- [ ] **B1 · 监听规则接 AI 判定** —— **施工图已出：`PLAN_B1.md`**（照它施工，别重新设计）。场景（用户原话概括，都是发 key 的人的反侦察手段）：
      ① key 中间插字符，正则筛不到；② key 后面加杂物，下一条消息说「删掉后面 xxx 才是真 key」，
      要跨消息理解；③ 用类脑社区的 `/下载` 命令发 txt 附件，key 在附件里——模型也提不到时
      要**提醒用户人工去看**。
      设计方向：规则里加「AI 复核」开关（命中宽条件后交模型判分/提取；模型说「附件/看不到」
      就发一条「需要你人工看」的通知）。注意硬规矩 6（每条模型路径都要有系统提示词）。
- [ ] **B2 · 「模型不太会按格式回答」出现太频繁**。用户用 DeepSeek v4 pro 都触发。
      两个方向一起做：①提示词写完整的格式指导+示例（few-shot）；②**提示词后处理严格模式**
      （用户实测酒馆 SillyTavern 的该选项让模型听话很多——研究一下它做了什么：
      大致是合并/规整 system·user·assistant 消息序列以适配严格 API）。
- [ ] **B3 · 内置提示词可见、可改、可导入导出**（酒馆预设风格）。现在提示词写死在
      文件里（"你是消息分流助手…只输出 JSON…"），用户想看到内容、能编辑、能以文件
      形式导入导出。走模板那套（cfg 存储 + dry_run 预览闸，照 extract_templates 抄）。
- [ ] **B4 · 模型参数可调**：温度、top_p、附加参数（自定义 JSON 透传）。默认**折叠**，
      一般人不用碰。这是「让模型老实的根本」（用户原话）。
- [ ] **B5 · 写一份「教模型写规则」的预设**并内置，给工作台的模型用，
      内容含 D 节的 Discord 层级命名约定（模型分不清频道/帖子就会写错规则）。
- [ ] **B6 · 用户反馈「模型可活动空间太少」**（模糊需求）。理解为：工作台模型的上下文/
      工具太受限。结合 HANDOFF 里「给模型加只读 export_extract_templates」一起考虑，
      不确定就在交付说明里问用户要具体场景。

## C. 功能 / 交互

- [x] **C1 · AI 工作台多开聊天** ✅ 已修（PLAN_A1C1.md：工作台左侧会话列表，
      新建/切换/改名/删除，「清空」改「新话题」，跟 A1 一起做）。
- [ ] **C2 · 新帖子的可见性问题 + 自动开标签页** —— **施工图已出：`PLAN_C2.md`**（含「不点开真的读不到」的完整答案）。用户问到点子上了：**浏览器旁听模式下，
      不点开的新帖子（Thread）确实读不到帖内消息**（DOM 里没有）。现状是只能看到
      「有新帖 + 标题」。方向：①扩展加「自动点开新帖」功能（要限制频率/数量）；
      ②让 AI 判断「某帖子很久没人发言」→ 提示可关掉该标签页腾资源。
      注意硬规矩 2：动 extension/ 必须 bump 版本 + EXT_MIN。
- [ ] **C3 · 模型选择的位置**：拉取模型后，「选哪个模型」出现在「默认模型」区而不是
      「接入的模型服务」区，用户觉得操作逻辑割裂。把模型选择挪到/同时显示在服务配置区。

## D. Discord 层级命名约定（用户让 Gemini 写的，已审）✅ 已定稿

**已定稿成 `docs/discord-命名约定.md`**（2026-08-01），全项目按它说话；
B5 的内置预设（`presets/教模型写规则.json`）引用它。下面是当初审稿的结论，留档。

结论：**5 层主干是对的，可以用**，程序、文档、给模型的提示词统一按这个说话。
采纳时做四点修订：

1. **私信（DM）不在这棵树里**，是树外的第 0 类。dcwatch 规则默认 `dm=False`，文档里要写明。
2. **语音频道也有文字聊天区**（Discord 后来加的），第 3 层语音频道的描述别写死「无须打字」；
   dcwatch 按文字消息对待即可。
3. 第 3 层补一种：**公告频道（Announcement）**，行为同文字频道。
4. 与 dcwatch 数据模型的对应关系写死进约定：第 4 层 Thread 在库里也是一条
   channel（带 parent），规则里「频道」条件既可填第 3 层也可填第 4 层的 ID；
   「有人开新帖」的 kind 只在第 3 层论坛/文字频道上触发。

原文（修订前）存档在本文件的 git 历史里即可，不必单独存。定稿后写成
`docs/discord-命名约定.md` 或并入 README，且 B5 的预设要引用它。

## 优先级建议（下个窗口从这里开工）

1. A2 + A3 + A5（监听是核心功能，坏了等于全坏）→ 各自修完跑回归
2. A4（转发出口锁死）
3. A1 + C1（工作台持久化 + 多会话，一起做）
4. B3 + B4 + B2（提示词可编辑 + 参数 + 严格模式，一个「模型接入」大轮）
5. B1（AI 复核规则，最大的新功能，单独一轮）
6. C2（扩展自动开帖，动 extension，单独一轮）
7. C3、B5、B6、D 定稿（穿插做）

## 流程想法（2026-07-31，用户提出，待确认后写入 AGENTS.md）
双模型分工：Claude 只做深度思考，把巨详细的实现方案写成 `PLAN_<任务名>.md` 推送到
GitHub，然后用户换 Kimi K2 来照方案写代码（快、便宜）。方案文件必须包含：改哪些文件、
函数签名、验收命令（让实现方自测）。NOW.md 指向当前 PLAN 文件。
