# 项目工作协议

本项目由 AI 协作完成，可能会跨多个会话 / 多个账号断续进行。
每次会话开始和结束时，请严格遵守以下流程，保证任务能无缝续接。

## 会话开始时（每次都要做）

0. **新账号 / 新窗口第一次接手：先读 `START_HERE.md`**，它把「头四件事」写死了
   （跑一次回归确认环境、读文件的顺序、跟用户要诊断包、凭据怎么要）。老窗口续着做的跳过这条。
1. 检查项目根目录是否存在 HANDOFF.md。
2. 如果存在，完整读取它，了解：
   - 上一轮做到哪一步了
   - 哪些任务已完成、哪些还没做
   - 有什么坑或注意事项（比如某个接口有速率限制、某个方案已经被否决过）
3. 如果 HANDOFF.md 里有未完成任务，从列表里挑第一项当前可以直接执行的任务开始做，不要重新规划或重复已完成的工作。
4. 如果 HANDOFF.md 不存在，说明是全新项目，正常按用户需求开始，并在第一次有实质进展后创建这个文件。

## 工作过程中

- 保持小步提交（commit），不要攒一大堆改动最后才提交一次。
- 每完成一个明确的子任务，就提交一次，commit message 简明描述做了什么。
- 遇到卡住、方案被否决、发现某条路走不通，随手记一句到 HANDOFF.md 的"注意事项"里，不要等到最后才补。

## 会话结束前（包括：任务全部完成 / 明显要断线 / 接近额度上限 / 用户说要结束时）

1. 更新 HANDOFF.md，用下面的固定结构覆盖写入：

# HANDOFF

## 最后更新时间
<写当前的日期时间>

## 当前状态
<一两句话概括项目整体进度>

## 已完成
- <任务1>
- <任务2>

## 未完成（按优先级排序）
- [ ] <下一步该做的第一件事，写清楚具体做什么，不要写"继续开发"这种模糊描述>
- [ ] <再下一件事>

## 注意事项 / 踩过的坑
- <比如：xxx 接口有速率限制，别一次调太快>
- <比如：xxx 方案试过不行，原因是……，不要再试>

2. git add -A && git commit -m "更新说明" && git push，确保代码和 HANDOFF.md 都推送到远程。
3. 不要在还没提交推送之前就结束会话。

## 总原则

把 HANDOFF.md 当成"交接给下一个自己"的备忘录来写——假设读它的人（也就是下一轮的你）对这个项目没有任何记忆，只能靠这份文件知道该干什么。
