# NOW —— 现在正在做的事

> 这一页是**进行中**的状态，粒度到"步"。`HANDOFF.md` 是做完一轮之后的总结，粒度到"轮"。
> 换窗口 / 换会话 / 换账号后聊天记录一句都不过来，**这一页就是替代品**。读完它你就知道
> 上一个窗口手停在哪儿、下一步该敲什么。
> **规矩：本页只保留「当前状态」一节 + 协议。做完一轮，把旧状态压缩成一两行挪进
> HANDOFF.md，别在这页往上摞新的「## 状态」——摞过 5 个，接手的人分不清哪个是真的。**

## 当前状态（2026-08-01）

**🔨 V 窗口正在做：C6 后台标签页防冻结/防回收**（用户 2026-08-01 新反馈，需求已进 BACKLOG）。
步骤：认领 → background.js 加 autoDiscardable=false + 巡检救回 → 测试桩同步+新断言 →
浏览器侧回归 → 三处钉齐 bump v1.11.2 → 四套版本敏感回归 → 双 zip 出包 → 文档收尾推送。

<details><summary>✅ U1 版本号对齐已做完已推送（U 窗口，一次干完没断）。</summary>
起因：用户 2026-08-01「拓展和程序版本必须对上，不然不好看」——v1.11.1 当时只 bump 了程序。
落盘：manifest 1.11.0→**1.11.1** + EXT_MIN 1.11.0→**1.11.1**（三处钉齐：程序/EXT_MIN/manifest 全 1.11.1）→
四套回归绿（ext 53 · diag 47 · tabs 53 · e2e 53，其余 8 套零版本引用安全）→ 双 zip 重打
（程序 27 文件 + 扩展 10 文件）进 release/（旧扩展 1.11.0 包已删）+ outputs/ →
全新解压冒烟：启动行「dcwatch v1.11.1 扩展应为 v1.11.1 以上，你这份是 v1.11.1」✓、/version 自证页三处全 1.11.1 ✓。
**新规矩已写进仓库 AGENTS.md 硬规矩 2：bump 程序 VERSION 时 manifest+EXT_MIN 必须同步 bump，
扩展 zip 跟着程序包每次都出——三处永远钉齐，不存在「只动程序」的版本。**

（旧状态折叠，详见 git 历史）
</details>

**之后（按序）：**
1. **用户侧：装 v1.11.1 真机验收**——这轮**程序扩展都要换**（扩展功能没动只是钉版本号，
   但也要重装一次才显示新号：覆盖 extension 文件夹 → chrome://extensions 点 ⟳ → Discord 页面 F5；
   覆盖程序时别删 dcwatch.db）：验 A7 收信箱（没规则=零收信，诊断包[3]段看「收信闸拦下 N 条」）、
   C5 输入框单光标、扩展卡片/弹窗显示 v1.11.1，连带老欠账（自动开帖默认关 / A6 密钥重填 / B1 AI 复核 / A2 A3 A5）
   → 导一份新诊断包发来。
2. 新需求先进 BACKLOG 再动工。

<details><summary>更早：v1.11.1 程序包（R 窗口）/ v1.11.0（C2 自动开帖）/ 文档重构（S 窗口）</summary>

v1.11.1 程序包：A7 收信闸（没规则=零收信）+ C5 光标修复落盘，全套 12 件 678 条绿（R 窗口）。
v1.11.0：C2 自动点开新帖+闲置自关（扩展+服务端）。S 窗口只做文档重构：NOW 去堆叠、
START_HERE 必读链 5→3 份 + 新开场消息模板。
</details>

## 窗口工作协议（用户 2026-07-31 定的，严格执行）
1. 用户消息有新需求 → **动任何代码之前**先写进 `BACKLOG.md` 并推送。
   窗口随时可能被额度掐断，没推送的需求＝丢了，用户说「继续」下个窗口就不知道继续什么
2. 挑定这个窗口的**一个**小任务后，先把本页「当前状态」改成「正在做 XX，步骤：…」并推送，再动手
3. 干活中每完成一步就 save 一次（save.py 会顺手推送），本页始终反映真实进度
4. 做完把本页改成「做完了 XX，下一件是 XX」并推送
5. 回复末尾报：✅ 本窗口完成 XX，已推送；下窗口发「继续」即可

## 并行窗口协议（2026-08-01 实测过三窗同仓，撞过）
用户可能把同一段接手提示词发给多个窗口。动手前必做：
1. `git fetch` 看远端最新几条 commit message——上面写着别人正在做哪一块，**别撞**；
2. 认领：把「我做哪一块」推一条状态（一行也行）；
3. 干完 `git pull --rebase` 再推。save.py 撞了会直接 fail（不强推，安全）。
经验教训：grep 别人的活别拿服务端字段名去搜前端/扩展（命名不同套，M 窗口误判过 C2 没做）。

## 双模型流程（用户 2026-07-31 启用）
Claude 窗口只做深度思考，方案写成 `PLAN_<任务>.md` 推送；实现窗口用户会换 Kimi K2 等快模型
照 PLAN 施工；再来一个 Claude 窗口只做 review（对照 PLAN 检查 diff + 回归）。
**实现窗口接到「继续」：读本页 → 读状态里指的 PLAN 文件 → 照施工图干，别自由发挥。**

## 换窗口/换账号丢什么（实测过，别再测一遍）
| 换窗口后 | 结果 |
|---|---|
| 工作区文件（.md/.py/.json/.zip/.wav/图片） | 在（**同账号**才在；换账号全没，只剩这个仓库） |
| 根 `AGENTS.md` | 在，且**每个新会话自动读**（换账号没有，靠开场消息重建，见 START_HERE §6） |
| `.bat` / `.sh` | **丢** |
| `.git/` | **丢**，工作区的 `claude/` 是死快照不是仓库 |
| `outputs/` | **整个清空** |
| `/tmp` / 聊天记录 | 没了 |

对策：干活 `git clone` 到 `/tmp/dcw`；每做完一步 `python3 agent/save.py "做了什么"`；
交付 zip 同时进 `release/` 和 `outputs/`；进行中状态写在本页。

## 历史窗口记录去哪了
2026-07-31 ~ 08-01 的 A–Q 十七个窗口的逐窗认领/分工/断点记录，原来堆在本页，
已随 git 历史保存（看 `git log -- NOW.md`，commit `69edaff` 之前的版本），
成果汇总在 `HANDOFF.md` 和 `BACKLOG.md` 的勾。别翻回来，以那两份为准。
