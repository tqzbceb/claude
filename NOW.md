# NOW —— 现在正在做的事

> 这一页是**进行中**的状态，粒度到"步"。`HANDOFF.md` 是做完一轮之后的总结，粒度到"轮"。
> 换窗口 / 换会话 / 换账号后聊天记录一句都不过来，**这一页就是替代品**。读完它你就知道
> 上一个窗口手停在哪儿、下一步该敲什么。
> **规矩：本页只保留「当前状态」一节 + 协议。做完一轮，把旧状态压缩成一两行挪进
> HANDOFF.md，别在这页往上摞新的「## 状态」——摞过 5 个，接手的人分不清哪个是真的。**

## 当前状态（2026-08-02 AO 窗口）

**（上一轮 E7+E8+E9 / v1.11.7 的细节已挪进 HANDOFF.md）**

**✅ 已做完：F1「名字→ID 名录」全部落盘，并已出 v1.12.1 修掉 v1.12.0 的一个真 bug**
（AK/AL/AM/AN/AO 五个窗口）：建规则不用再手抄 18 位 ID —— 扩展逛 Discord 时顺手把侧栏的
服务器/分类/频道/子区名字和消息作者名抄进名录并上报；服务端 `names` 表 + `/api/lookup`
模糊查（候选带面包屑 `测试服 › 信息区` 和「多久前见过」）；规则页四个栏直接打名字弹候选；
工作台模型多了 `find_target`，能自己查名录再找用户确认。名字不唯一**永远让用户挑，不替他猜**；
只认浏览器见过的，没见过的仍然贴链接/ID。学习挂在收信路径最前面，**收信闸拦下也照学**，
所以「没规则不收信 → 第一条规则 ID 没处抄」这个鸡生蛋也顺手破了。

**v1.12.1 修的是 v1.12.0 已发布的真 bug**：Discord 的频道链接也在 `<a>` 里，
`harvestGuilds` 把频道名当服务器名写到了服务器 ID 上 —— 结果**打服务器名搜不到候选**。
是 AN 窗口给 `content_test.mjs` 补第四套 `runNames()`（32 条常驻回归）时抓出来的。

- 回归：服务端 **14 件 870 条全绿**（本轮实测；文档里的旧数 858 已按实测改正，
  e2e.py 是 60 条不是 48、e2e_diag 是 50 条不是 47），
  扩展侧 CDP **4 套 139 条全绿**（run 49 / runFresh 16 / runTabs 42 / runNames 32）。
- 三处钉齐 v1.12.1（server.py `VERSION`/`EXT_MIN` + `extension/manifest.json`）；
  双 zip 在 `release/` + `outputs/`（旧 1.12.0 已删，程序 27 文件 / 扩展 11 条目，
  逐条跟上版清单对齐）；全新解压冒烟全过（启动首行、`/version`、`/api/presets`、
  `/api/names`、`/api/lookup`）。
- 顺手修的真 bug（历史）：`find_target` 里误写 `a.get`（每次调用必崩）；
  `mockllm` system 只记 6000 字造成 e2e_wiz 8 条假红。
- 顺手修文案：`extension/怎么装.txt` 里写死的「版本号现在应该是 1.8.0」改成跟程序钉齐。

**⚠️ 踩过的坑（AO 窗口）**：AN 之后有一个窗口做完 1.12.1 的 bump 和回归但**没推送就被掐断**，
这一轮是把那批活重做的。→ 每做完一步立刻推。

**🔨 下一件事**：BACKLOG 里 F1 已打勾，暂无认领中的任务。等用户真机验收结果，
或用户提新需求（新需求先写进 `BACKLOG.md` 并推送，再动代码）。

**等用户真机验收（v1.12.1，程序必换、⚠️ 扩展也必须重装）**：
1. 解压覆盖 → `启动.bat` → 黑窗口第一行 `dcwatch v1.12.1`（**别删 `dcwatch.db`**）；
2. **扩展必须重装**：覆盖 `extension` 文件夹 → `chrome://extensions` 点 ⟳ → Discord 页面 F5。
   不重装的话名录是空的，「填名字」查不到东西；
3. **F1 主验**：在 Discord 里正常逛几分钟（让扩展把侧栏名字抄下来）→ 回 dcwatch 建规则，
   频道栏直接打「公告」之类的名字 → 应该弹出候选，每条写着它在哪个服/哪个分类 → 挑一个即可；
   **服务器栏也打一下服务器名**（这正是 v1.12.1 修的那条，1.12.0 上搜不到）；
4. 工作台再说一句「帮我盯测试服的公告频道」→ 模型应自己查名录、列候选让你确认，
   而不是回「请先复制频道 ID」；
5. 顺路补验旧账：模型拉列表（先在「设置」填代理端口如 `http://127.0.0.1:7890`）、
   多目标开服监听、行内表情、自动开帖新最小化窗口、E7 输入框单光标、E8 不再误报扩展旧版；
6. 验完导一份新诊断包发来。**规则 0 条时收信闸拦信是预期行为**（没规则=不收信）。

## 窗口工作协议（用户 2026-07-31 定的，严格执行）
1. 用户消息有新需求 → **动任何代码之前**先写进 `BACKLOG.md` 并推送。
   窗口随时可能被额度掐断，没推送的需求＝丢了，用户说「继续」下个窗口就不知道继续什么
2. 挑定这个窗口的**一个**小任务后，先把本页「当前状态」改成「正在做 XX，步骤：…」并推送，再动手
3. 干活中每完成一步就 save 一次（save.py 会顺手推送），本页始终反映真实进度
4. 做完把本页改成「做完了 XX，下一件是 XX」并推送
5. 回复末尾报：✅ 本窗口完成 XX，已推送；下窗口发「继续」即可

## 并行窗口协议（2026-08-01 实测过三窗同仓，撞过）
用户可能把同一段接手提示词发给多个窗口。动手前必做：
1. `git fetch` 看远端最新几条 commit message——上面写着别人正在做哪一块，**别撞**；
2. 认领：把「我做哪一块」推一条状态（一行也行）；
3. 干完 `git pull --rebase` 再推。save.py 撞了会直接 fail（不强推，安全）。
经验教训：grep 别人的活别拿服务端字段名去搜前端/扩展（命名不同套，M 窗口误判过 C2 没做）。

## 双模型流程（用户 2026-07-31 启用）
Claude 窗口只做深度思考，方案写成 `PLAN_<任务>.md` 推送；实现窗口用户会换 Kimi K2 等快模型
照 PLAN 施工；再来一个 Claude 窗口只做 review（对照 PLAN 检查 diff + 回归）。
**实现窗口接到「继续」：读本页 → 读状态里指的 PLAN 文件 → 照施工图干，别自由发挥。**

## 换窗口/换账号丢什么（实测过，别再测一遍）
| 换窗口后 | 结果 |
|---|---|
| 工作区文件（.md/.py/.json/.zip/.wav/图片） | 在（**同账号**才在；换账号全没，只剩这个仓库） |
| 根 `AGENTS.md` | 在，且**每个新会话自动读**（换账号没有，靠开场消息重建，见 START_HERE §6） |
| `.bat` / `.sh` | **丢** |
| `.git/` | **丢**，工作区的 `claude/` 是死快照不是仓库 |
| `outputs/` | **整个清空** |
| `/tmp` / 聊天记录 | 没了 |

对策：干活 `git clone` 到 `/tmp/dcw`；每做完一步 `python3 agent/save.py "做了什么"`；
交付 zip 同时进 `release/` 和 `outputs/`；进行中状态写在本页。

## 历史窗口记录去哪了
2026-07-31 ~ 08-01 的 A–Q 十七个窗口的逐窗认领/分工/断点记录，原来堆在本页，
已随 git 历史保存（看 `git log -- NOW.md`，commit `69edaff` 之前的版本），
成果汇总在 `HANDOFF.md` 和 `BACKLOG.md` 的勾。别翻回来，以那两份为准。
