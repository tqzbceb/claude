# NOW —— 现在正在做的事

> 这一页是**进行中**的状态，粒度到"步"。`HANDOFF.md` 是做完一轮之后的总结，粒度到"轮"。
> 换窗口 / 换会话 / 换账号后聊天记录一句都不过来，**这一页就是替代品**。读完它你就知道
> 上一个窗口手停在哪儿、下一步该敲什么。
> **规矩：本页只保留「当前状态」一节 + 协议。做完一轮，把旧状态压缩成一两行挪进
> HANDOFF.md，别在这页往上摞新的「## 状态」——摞过 5 个，接手的人分不清哪个是真的。**

## 当前状态（2026-08-02 AE 窗口）

**（上一轮 E5+E6 / v1.11.6 的细节已挪进 HANDOFF.md）**

**✅ 这一轮：E7 + E8 + E9 全部落盘，v1.11.7 已出包已推送**（AG/AH/AI/AJ 窗口）：
- **E7**（输入框发完一条又变两处光标）：从根上治 —— 发送后把 textarea 的值/高度归零重算，
  输入法 Enter 不再漏换行；**手动拉高（resize:vertical）还给用户**，不是靠删功能糊过去。
- **E8**（诊断/界面误报「扩展是旧版」）：改成跟**磁盘上的实际扩展版本**比，并点名
  「哪个标签页在跑旧脚本、按 F5 就行」，不再叫用户白重装。
- **E9**（模型只会教步骤不会动手）：`WB_TOOLS` 补写工具 —— 开服监听 CRUD + 立刻现探一次、
  本机提醒开关/免打扰/分数门槛、自动开帖开关与上限、从已拉到的模型里选默认模型；
  `WB_WRITE_TOOLS` 确认闸 / `WB_TOOLS_HOWTO` / `WORKBENCH_SYS` 功能清单同步（硬规矩 6）。
  红线不动：API Key、转发出口密址、导入规则永不给模型写（硬规矩 10）。
- 服务端 13 件 **785 条全绿**（e2e_wb 189 含 E9 专项：只认配置真的变了）；三处钉齐 v1.11.7；
  双 zip 在 `release/`（旧 1.11.6 已删）+ `outputs/`；全新解压冒烟：启动行 `dcwatch v1.11.7`、
  `/version` 自证页、presets、`/api/watch` 全过。扩展这轮只动 manifest 版本号，JS 零改动。

**🔨 正在做（AK 窗口，2026-08-02）：F1 名字→ID 名录**（用户原话：「我输入用户名字或者
频道名字他没法监听啊，必须要我给 id，难道不能他自己根据名字去找一下，再找我确认是不是？」）。
步骤：①扩展攒名录（侧栏 href 带 ID + 消息头作者）并上报 → ②服务端建表存 + `/api/lookup` 模糊查
（候选带所属服务器/分类）→ ③工作台 `find_target` 工具 + 提示词/功能清单同步（硬规矩 6）→
④规则页频道/用户栏改可搜索选择器 → ⑤测试（服务端专项 + 扩展侧断言）→ ⑥出 **v1.12.0** 双 zip
（**动了扩展，三处钉齐、用户必须重装扩展**）。每步 save 一次。

**等用户真机验收**（v1.11.7，程序必换、扩展可选）：
1. 解压覆盖 → `启动.bat` → 黑窗口第一行 `dcwatch v1.11.7`（别删 `dcwatch.db`）；
2. **E9 主验**：工作台直接说「帮我盯 xxx 网址开没开，5 分钟探一次」→ 模型应**自己建好**并回
   「已建好」，而不是念一二三步；再说「关掉声音」「打开自动开帖」也应它自己动手；
3. **E7**：发出一条消息后输入框收回单行，只有一处能点出光标（右下角拉高手柄还在，是故意留的）；
4. **E8**：诊断包不再误报「扩展是旧版」；真有旧标签页时会点名哪个页面按 F5；
5. 顺路补验旧账：模型拉列表（先在「设置」填代理端口如 `http://127.0.0.1:7890`）、多目标开服监听、
   行内表情、自动开帖新最小化窗口；
6. 验完导一份新诊断包发来。**他现在规则 0 条，收信闸拦信是预期行为**（没规则=不收信）。

## 窗口工作协议（用户 2026-07-31 定的，严格执行）
1. 用户消息有新需求 → **动任何代码之前**先写进 `BACKLOG.md` 并推送。
   窗口随时可能被额度掐断，没推送的需求＝丢了，用户说「继续」下个窗口就不知道继续什么
2. 挑定这个窗口的**一个**小任务后，先把本页「当前状态」改成「正在做 XX，步骤：…」并推送，再动手
3. 干活中每完成一步就 save 一次（save.py 会顺手推送），本页始终反映真实进度
4. 做完把本页改成「做完了 XX，下一件是 XX」并推送
5. 回复末尾报：✅ 本窗口完成 XX，已推送；下窗口发「继续」即可

## 并行窗口协议（2026-08-01 实测过三窗同仓，撞过）
用户可能把同一段接手提示词发给多个窗口。动手前必做：
1. `git fetch` 看远端最新几条 commit message——上面写着别人正在做哪一块，**别撞**；
2. 认领：把「我做哪一块」推一条状态（一行也行）；
3. 干完 `git pull --rebase` 再推。save.py 撞了会直接 fail（不强推，安全）。
经验教训：grep 别人的活别拿服务端字段名去搜前端/扩展（命名不同套，M 窗口误判过 C2 没做）。

## 双模型流程（用户 2026-07-31 启用）
Claude 窗口只做深度思考，方案写成 `PLAN_<任务>.md` 推送；实现窗口用户会换 Kimi K2 等快模型
照 PLAN 施工；再来一个 Claude 窗口只做 review（对照 PLAN 检查 diff + 回归）。
**实现窗口接到「继续」：读本页 → 读状态里指的 PLAN 文件 → 照施工图干，别自由发挥。**

## 换窗口/换账号丢什么（实测过，别再测一遍）
| 换窗口后 | 结果 |
|---|---|
| 工作区文件（.md/.py/.json/.zip/.wav/图片） | 在（**同账号**才在；换账号全没，只剩这个仓库） |
| 根 `AGENTS.md` | 在，且**每个新会话自动读**（换账号没有，靠开场消息重建，见 START_HERE §6） |
| `.bat` / `.sh` | **丢** |
| `.git/` | **丢**，工作区的 `claude/` 是死快照不是仓库 |
| `outputs/` | **整个清空** |
| `/tmp` / 聊天记录 | 没了 |

对策：干活 `git clone` 到 `/tmp/dcw`；每做完一步 `python3 agent/save.py "做了什么"`；
交付 zip 同时进 `release/` 和 `outputs/`；进行中状态写在本页。

## 历史窗口记录去哪了
2026-07-31 ~ 08-01 的 A–Q 十七个窗口的逐窗认领/分工/断点记录，原来堆在本页，
已随 git 历史保存（看 `git log -- NOW.md`，commit `69edaff` 之前的版本），
成果汇总在 `HANDOFF.md` 和 `BACKLOG.md` 的勾。别翻回来，以那两份为准。
